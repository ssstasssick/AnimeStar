<h2 class="text-white">Детали пользователя</h2>
<div class="fullInfAboutAnime">
    <div>
        <dl class="row text-white">
            <dt class="col-sm-2">Имя</dt>
            <dd class="col-sm-10">@ViewBag.User.UserName</dd>

            <dt class="col-sm-2">Email</dt>
            <dd class="col-sm-10">@ViewBag.User.Email</dd>

            <dt class="col-sm-2">Роль</dt>
            <dd class="col-sm-10">@ViewBag.User.Role</dd>
        </dl>
    </div>

    <p>
        <a asp-action="EditUser" asp-route-id="@ViewBag.User.Id">Изменить</a> |
        <a asp-action="Users">Вернуться к списку</a>
    </p>
</div>